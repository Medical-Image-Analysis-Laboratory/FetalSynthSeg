# link augmentations from augmentations yaml
defaults:
  - _self_
  - augmentations_online
  - fetsynthgen
_target_: src.data.feta_datamodule.FetaDataModuleOnlineSynth

# type of images and splits to use for training and validation
train: synth # synth feta

validate: feta # synth feta feta+synth

# split of the data to use for training and validation
# defines in splits.csv ['Splits'] column
train_split: ['CHUV - rec-mial']

valid_split: ['KISPI - rec-mial']

# path to the datasets tha should countain bids style folders with images


segm_path: /media/vzalevskyi/data/FETA_challenge/merged_feta_spinabifida/derivatives/resampled05
seed_path: /media/vzalevskyi/data/FETA_challenge/merged_feta_spinabifida/derivatives/seeds
feta_path: /media/vzalevskyi/data/FETA_challenge/merged_feta_spinabifida/derivatives/resampled05
synth_path: /media/vzalevskyi/data/FETA_challenge/merged_feta_spinabifida/derivatives/resampled05
split_file: /home/vzalevskyi/projects/fetalSynthSegm/data/splits/feta_splits.csv # used for training purposes to define subjects belonging to each split

batch_size: 1
num_workers: 10
pin_memory: False
synth_val_size: 100

